# Opt in by installing this as /etc/systemd/system/reachd.service.d/exo-connector.conf.
# Connector READY means local serving only. The upstream cluster must be ready.
[Unit]
Wants=reach-exo-connector.service
After=reach-exo-connector.service
