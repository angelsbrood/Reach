#!/bin/sh
set -eu

case "${1:-}" in
    remove|purge)
        rm -rf -- /var/lib/reach /run/reach
        ;;
esac
if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload || true
fi

exit 0
